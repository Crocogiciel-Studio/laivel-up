-- Seed data for a fresh local stack.
--
-- The read-only references -- the AIDD reference grid and the four sample
-- profiles, as `is_template = true` rows -- ship in the migration
-- `20260831170000_seed_templates.sql` (generated by
-- `scripts/build-template-seed.mjs`), so a local `db reset` and a first
-- `db push` both get them. Nothing else is seeded.
