{
  "name": "@laivel-up/studio-db",
  "version": "0.0.0",
  "private": true,
  "description": "Studio database: schema, RLS, migrations, and the seeded reference templates",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "db:new": "supabase migration new",
    "db:link": "supabase link",
    "db:push": "supabase db push",
    "db:test": "supabase db reset && psql \"${SUPABASE_DB_URL:-postgresql://postgres:postgres@127.0.0.1:54322/postgres}\" -v ON_ERROR_STOP=1 -f supabase/tests/rls_smoke.sql",
    "db:test:bare": "scripts/rls-test-bare.sh",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:status": "supabase status",
    "db:reset": "supabase db reset",
    "db:seed:templates": "pnpm -C ../core build && node scripts/build-template-seed.mjs",
    "templates:check": "node scripts/build-template-seed.mjs --check"
  },
  "devDependencies": {
    "supabase": "2.2.1"
  }
}
